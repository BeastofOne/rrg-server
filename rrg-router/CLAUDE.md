# RRG Router
Request routing service that classifies intents and dispatches to appropriate handlers.
- **Tech:** Python, Nix flake
- **Entry:** `app.py`
- **Key files:** `claude_llm.py` (LLM calls), `config.py` (settings)
- **Run:** `nix develop` then `python app.py`
